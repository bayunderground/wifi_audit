[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name='wifi-audit'
version='0.1.0'
dependencies=['PyYAML']

[tool.setuptools.packages.find]
include = ["audit*"]
